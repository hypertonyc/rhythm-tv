// Package web хранит браузерный клиент, который сервер отдаёт на «/».
//
// Это дословная копия строки из appHtml() в legacy/server.mjs: шаблонный литерал
// без единой подстановки, поэтому он переносится байт в байт. Файл обязан
// оставаться без завершающего перевода строки — Content-Length считается по нему,
// а редактор добавляет \n молча (см. embed_test.go).
package web

import _ "embed"

//go:embed index.html
var IndexHTML []byte
