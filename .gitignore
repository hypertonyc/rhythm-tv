.DS_Store
.env

# Результат сборки Tizen-клиента
Debug/
Release/
.buildResult/
.sign/
*.wgt

# Генерируется задачей сборки из DEFAULT_SERVER в .env (адрес с приватным токеном)
client/js/config.js

# Временные артефакты (которые не нужны в репозитории)
data/

# Настройки прода для VPS: копия лежит на сервере как /srv/tms/.env.
# Шаблон под контролем версий — deploy/.env.example.
deploy/.env
